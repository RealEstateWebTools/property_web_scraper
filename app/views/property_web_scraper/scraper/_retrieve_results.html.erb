<p>Property info retrieved:</p>
<div class="mb-3">
  <%= link_to "Single Page view", single_property_view_path(url: @listing.import_url), class: "btn btn-success me-2" %>
  <%= link_to "JSON view", retriever_as_json_path(url: @listing.import_url), class: "btn btn-success" %>
</div>
<table class="table table-striped">
  <tbody>
    <% @listing_attributes.each do |listing_attribute| %>
      <tr>
        <th><%= listing_attribute %></th>
        <td><%= @listing[listing_attribute] %></td>
      </tr>
    <% end %>
    <tr>
      <th>Url</th>
      <td><%= link_to @listing.import_url, @listing.import_url, target: "_blank" %></td>
    </tr>
  </tbody>
</table>
<div class="mb-3">
  <strong>Images</strong>
  <% @image_urls.each do |listing_image_url| %>
    <div>
      <%= link_to listing_image_url, listing_image_url, target: "_blank" %>
    </div>
  <% end %>
</div>
<div>
  <%= link_to "Single Page view", single_property_view_path(url: @listing.import_url), class: "btn btn-success me-2" %>
  <%= link_to "JSON view", retriever_as_json_path(url: @listing.import_url), class: "btn btn-success" %>
</div>
