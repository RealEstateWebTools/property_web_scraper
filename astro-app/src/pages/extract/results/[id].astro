---
// Redirect to the unified listing detail page.
// All links to /extract/results/:id are preserved — they just land here now.
const { id } = Astro.params;
return Astro.redirect(`/listings/${id}`, 301);
---
